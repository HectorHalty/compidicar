PROGRAMA%SUMA {
    ## Contiene if, while, funcion , trunc, lambda, print ##
    uint BASE, EXP;
    BASE = 2UI;
    EXP = 3UI;

    uint POTENCIA (uint BASE, cv uint EXPONENTE) {
        uint CONTADOR, RES;
        CONTADOR:= 1UI; 
        RES:= 0UI;
        if (BASE < EXPONENTE ){
            print(BASE);
            print(EXPONENTE);
        }endif;
        
        do {
            RES:= BASE * BASE + RES; 
            CONTADOR := CONTADOR + 1UI;
        } while ( CONTADOR < EXPONENTE);

        return(RES);
    }
    uint POT;
    POT:=POTENCIA(trunc(3.0)->EXPONENTE, BASE ->BASE);

    print(POT);
    ##vemos ademas el funcionamiento de lambda ##
    uint B;
    B:= 3UI;
    (uint A){print(A); A:=B; print(A);}(1UI)

    
}
