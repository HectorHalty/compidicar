IFCUERPO{
    ## sin cuerpo en ambos lados ##
    uint A, B;
    A:= 1UI;
    B:= 2UI;
    if (A < B)
        
    else
        
    endif;
    print(A);
    ## sin cuerpo en else##
    if (A < B)
        A:= B;
    else
        
    endif;
    print(A);
    ## sin cuerpo en then ##
    if (A < B)
        
    else
        A:=B;
    endif;
    print(A);
}