MAIN {
    ## Falta ambos parentesis ## 
    uint A, B;
    A:= 1UI;
    B:= 2UI;
    if A < B
        A:= B;
    else
        B:= A;
    endif;
    print(A);

    ##Falta parentesis izq##

    A:= 1UI;
    B:= 2UI;
    if A < B)
        A:= B;
    else
        B:= A;
    endif;
    print(A);
    ##Falta parentesis der##

    A:= 1UI;
    B:= 2UI;
    if (A < B
        A:= B;
    else
        B:= A;
    endif;
    print(A);
}